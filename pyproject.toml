[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mypvm"
version = "0.1.0"
description = "Python Virtual Machine"
requires-python = ">=3.14"

[project.scripts]
pvm = "mypvm.__main__:main"